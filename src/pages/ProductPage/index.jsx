import React from "react";
import { useQuery } from "@apollo/client";

const ClothCard = () => {
  return <div></div>;
};

const ProductPage = () => {
  // array of ClothCard
  // const length of array of ClothCard (maybe via using useState)
  return;
};

export default ProductPage;
